في Node.js، يُعد كل من Stream وBuffer من المفاهيم المهمة جدًا، وخاصة عند التعامل مع نظام الملفات والشبكات ومعالجة البيانات الكبيرة.
الـStream هو آلية تسمح بمعالجة البيانات على أجزاء صغيرة بدلًا من تحميل البيانات كاملة في الذاكرة دفعة واحدة.
لنفترض أن لديك ملفًا كبيرًا حجمه 1 جيجابايت.
إذا قمت بتحميل الملف كاملًا إلى الذاكرة، فقد يستخدم ذلك كمية كبيرة من ذاكرة RAM.
ولكن عند استخدام Stream، يمكن قراءة الملف على شكل أجزاء صغيرة تسمى Chunks.
تتم معالجة Chunk واحدة، ثم تتم معالجة الـChunk التالية.
لهذا السبب تساعد Streams في تقليل استخدام الذاكرة وبناء تطبيقات أكثر كفاءة وقابلية للتوسع.
في Node.js توجد أربعة أنواع رئيسية من Streams.
وهي Readable وWritable وDuplex وTransform.
يُستخدم Readable Stream لقراءة البيانات.
ويُستخدم Writable Stream لكتابة البيانات.
أما Duplex Stream فيستطيع قراءة البيانات وكتابتها في الوقت نفسه.
بينما يستطيع Transform Stream تعديل البيانات أو تحويلها أثناء قراءتها أو كتابتها.
عند قراءة الملفات، يكون Readable Stream هو النوع الأكثر استخدامًا.
يمكن استخدام وحدة `fs` في Node.js لإنشاء Readable Stream من ملف.
ولهذا الغرض نستخدم الدالة `fs.createReadStream()`.
على سبيل المثال، يمكن استخدام `fs.createReadStream('bigdata.txt')` لقراءة ملف على شكل Stream.
في هذه الحالة، لا يقوم Node.js بتحميل الملف كاملًا إلى الذاكرة دفعة واحدة.
بل يقرأ كمية محددة من البيانات من الملف.
تُسمى هذه البيانات عادةً Chunk.
عند وصول Chunk جديدة، يقوم Stream بإطلاق حدث يسمى `data`.
يمكن الاستماع إلى هذا الحدث باستخدام `.on('data', callback)`.
على سبيل المثال، عند كتابة `ourReadStream.on('data', (chunk) => {})` سيتم تنفيذ الـcallback في كل مرة تصل فيها Chunk جديدة.
هنا تمثل `chunk` الجزء الذي تم الحصول عليه حاليًا من البيانات.
في Node.js تكون هذه الـChunk عادةً على شكل Buffer.
الـBuffer هو جزء من الذاكرة يُستخدم لتخزين البيانات الثنائية بشكل مؤقت.
لغة JavaScript تتعامل عادةً مع String وNumber وObject وArray.
ولكن الكثير من البيانات التي يتعامل معها الحاسوب تكون في الأصل على شكل بيانات ثنائية.
مثل الصور والفيديوهات والملفات والبيانات الصوتية.
ولهذا السبب يوفر Node.js كائن Buffer للتعامل مع البيانات الثنائية.
الـBuffer يمثل البيانات الثنائية الخام.
لذلك قد لا تكون الـChunk التي يتم الحصول عليها من File Stream قابلة للقراءة مباشرةً كنص.
إذا كانت بيانات الملف عبارة عن نص، يمكن استخدام `chunk.toString()` لتحويلها إلى String.
على سبيل المثال، سيقوم `console.log(chunk.toString())` بعرض النص المقروء من الـChunk في وحدة التحكم.
في هذه العملية، تتم أولًا قراءة البيانات من الملف.
ثم تصل البيانات على شكل Chunk.
وتكون الـChunk في صورة Buffer.
بعد ذلك تقوم `toString()` بتحويل البيانات الثنائية الموجودة داخل Buffer إلى تمثيل نصي.
ثم تقوم `console.log()` بطباعة النص في Terminal.
لفهم العملية بشكل أفضل، يمكن تخيل ملف كبير يسمى `bigdata.txt`.
لنفترض أن هذا الملف يحتوي على آلاف الأسطر من النص.
عند استخدام `createReadStream()`، لن يقرأ Node.js الملف كاملًا دفعة واحدة.
بل سيقرأ جزءًا من البيانات من الملف.
عند وصول البيانات، يتم تشغيل حدث `data`.
ثم تصبح هذه الـChunk متاحة داخل دالة الـcallback.
بعد ذلك يتم طباعة الـChunk في وحدة التحكم.
ثم يقوم الـStream بقراءة الـChunk التالية.
ومرة أخرى يتم تشغيل حدث `data`.
ومرة أخرى يتم تنفيذ الـcallback.
تستمر هذه العملية حتى تتم قراءة الملف بالكامل.
أي أن الملف الكبير لا تتم معالجته كعملية واحدة، بل تتم معالجته على شكل عدد كبير من العمليات الصغيرة.
وهذه واحدة من أهم أفكار الـStream.
تعمل Streams بشكل جيد جدًا مع البرمجة غير المتزامنة والمبنية على الأحداث.
يعتمد Node.js نفسه على بنية Event-Driven Architecture.
لذلك يتوافق سلوك Streams القائم على الأحداث بشكل طبيعي مع بنية Node.js.
بالإضافة إلى حدث `data`، توجد أحداث مهمة أخرى في Readable Stream.
يتم تشغيل حدث `end` عندما لا تكون هناك بيانات أخرى متبقية في الـStream.
أي أنه عند الانتهاء من قراءة الملف بالكامل، يتم تشغيل حدث `end`.
أما حدث `error` فقد يتم تشغيله عند حدوث مشكلة أثناء عملية القراءة.
على سبيل المثال، إذا حدث خطأ أثناء الوصول إلى الملف، يمكن استخدام حدث `error` للتعامل معه.
ويمكن أيضًا استخدام حدث `close` عند إغلاق الـStream.
لذلك يمكن أن تكون دورة حياة Readable Stream الأساسية على النحو التالي.
أولًا يتم إنشاء الـStream.
ثم تبدأ البيانات في الوصول.
يتم تشغيل حدث `data` لكل Chunk من البيانات.
بعد قراءة جميع البيانات، يتم تشغيل حدث `end`.
وفي حالة حدوث مشكلة، قد يتم تشغيل حدث `error`.
هذه البنية مفيدة جدًا في التطبيقات الكبيرة.
لأن التطبيق لا يحتاج إلى الانتظار حتى يتم الحصول على جميع البيانات قبل البدء في معالجتها.
يمكن معالجة البيانات فور وصولها.
ويمكن تسمية ذلك بالمعالجة التدريجية أو Incremental Processing.
لنفترض أن مستخدمًا يقوم برفع ملف حجمه 500 ميجابايت إلى خادم.
إذا حاول الخادم تحميل الملف كاملًا إلى الذاكرة، فقد يرتفع استخدام الذاكرة بشكل كبير.
لكن باستخدام Stream، يمكن استقبال أجزاء صغيرة من الملف ومعالجتها تدريجيًا.
وبنفس الطريقة يمكن استخدام Stream أثناء تنزيل الملفات الكبيرة.
يمكن معالجة بيانات استجابة HTTP على شكل Chunks.
وتعمل الفكرة الأساسية نفسها في Video Streaming.
لا يحتاج المستخدم إلى انتظار تنزيل الفيديو بالكامل حتى يبدأ في مشاهدته.
بل يمكنه مشاهدة الفيديو أثناء وصول البيانات إليه.
وهنا تكون عملية تدفق البيانات المستمرة ومعالجة البيانات على شكل Chunks أمرًا مهمًا جدًا.
لا تُستخدم Streams مع الملفات فقط.
بل تُستخدم أيضًا مع اتصالات الشبكة وطلبات HTTP واستجابات HTTP وإدخال وإخراج العمليات.
في Node.js، يُعد `process.stdin` مثالًا على Readable Stream.
ويُعد `process.stdout` مثالًا على Writable Stream.
كما تعتمد طلبات HTTP واستجابات HTTP أيضًا على مفهوم Streams.
ولهذا السبب من المهم فهم Streams جيدًا عند تعلم Backend Development.
الـBuffer والـStream مرتبطان ببعضهما بشكل وثيق، ولكنهما ليسا الشيء نفسه.
الـStream هو الآلية التي تحدد كيفية انتقال البيانات.
أما الـBuffer فهو بنية في الذاكرة تُستخدم لتخزين جزء من البيانات الثنائية مؤقتًا.
وبطريقة بسيطة، يمكن القول إن Stream ينقل البيانات، بينما Buffer يحتفظ بجزء من البيانات أثناء المعالجة.
عندما تصل البيانات من خلال Stream، قد تصل هذه البيانات على شكل Buffer.
بعد ذلك يمكن للتطبيق معالجة الـBuffer حسب الحاجة.
إذا كانت البيانات نصية، يمكن استخدام `toString()` لتحويلها إلى String.
أما إذا كانت البيانات ثنائية، فيمكن معالجتها مباشرةً على شكل Buffer.
على سبيل المثال، عند معالجة الصور، يمكن الحصول على المعلومات الثنائية من Buffer.
ليس من الضروري أن يكون حجم كل Chunk في Stream متساويًا دائمًا.
يمكن أن يعتمد حجم الـChunk على نظام التشغيل والتخزين المؤقت الداخلي وإعدادات الـStream.
لذلك يجب على المطور ألا يتعامل مع Chunk على أنها سطر ثابت أو وحدة منطقية ثابتة.
فالـChunk ليست سوى جزء من البيانات.
قد تحتوي Chunk واحدة على سطر واحد.
وقد تحتوي Chunk أخرى على عدة أسطر.
وقد تنتهي Chunk في منتصف أحد الأسطر.
لذلك يجب تصميم منطق التطبيق بعناية عند التعامل مع البيانات على شكل Chunks.
وهذا الأمر مهم بشكل خاص عند معالجة تنسيقات ملفات مخصصة أو بروتوكولات خاصة.
يمكن استخدام حدث `data` في Readable Stream لقراءة الملفات بطريقة بسيطة.
ولكن في التطبيقات الحقيقية يجب أيضًا الاهتمام بمعالجة الأخطاء وإدارة الموارد بشكل صحيح.
من الأمثلة الأساسية استخدام `const fs = require('fs')` لاستيراد وحدة File System.
بعد ذلك يمكن إنشاء Stream باستخدام `const ourReadStream = fs.createReadStream(__dirname + '/bigdata.txt')`.
ثم يمكن استقبال البيانات باستخدام `ourReadStream.on('data', (chunk) => { console.log(chunk.toString()) })`.
لكل جزء من هذا الكود مسؤولية محددة.
يوفر `fs` إمكانية التعامل مع نظام الملفات.
وتقوم `createReadStream()` بإنشاء Readable Stream.
ويستمع `on('data')` إلى وصول Chunks جديدة من البيانات.
ويمثل `chunk` البيانات التي وصلت في تلك اللحظة.
وتقوم `toString()` بتحويل Buffer إلى نص قابل للقراءة.
أما `console.log()` فيعرض النص الناتج في Terminal.
يمكن لهذا المثال الصغير أن يوضح مفهومًا كبيرًا جدًا في Node.js.
فالـStream يهتم بشكل أساسي بتدفق البيانات.
بينما يهتم الـBuffer بالتمثيل الثنائي المؤقت للبيانات.
وعند استخدامهما معًا يمكن التعامل مع كميات كبيرة من البيانات بكفاءة.
ولهذا السبب تُعد Streams وBuffers من المفاهيم المهمة جدًا في نظام الملفات والشبكات في Node.js.
وبالنسبة لمطور Backend، فإن فهم Streams وBuffers يجعل فهم File Upload وFile Download واتصالات HTTP ومعالجة البيانات في الوقت الحقيقي أسهل بكثير.
